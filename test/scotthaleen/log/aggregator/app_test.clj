(ns scotthaleen.log.aggregator.app-test
  (:require [clojure.test :refer :all]
            [scotthaleen.log.aggregator.app-test :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))
